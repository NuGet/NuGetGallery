@helper DisplayRating(float averageRating) {
    int flooredAverageRating = (int)averageRating;
    
    <div class="stars-current-result stars-value-@flooredAverageRating" style="display:inline;">
        @for (int i = 1; i < 6; i++) {
            //TODO: Probably should be a helper.
            <span class="star-@i a-star" title="Average Rating: @averageRating">
                @if (i >= flooredAverageRating) {<text>*</text>} else {<text>-</text>}
            </span>
        }
    </div>
}